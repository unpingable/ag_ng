# Modern regeneration boundary

Implementation candidate; no new VM execution or inherited qualification.
Current contracts, GLASS-HERON runner and both generators require native
`nq-ng.campaign-stage-*` evaluation/replay via Nightshift's `--nq-executable`.
NQ is factual, Nightshift supplies applicability, AG authorizes and Docket owns
execution. Old packets and qualification records remain historical at old pins.

VELVET regeneration requires new `RETIREMENT_SPECIMEN_OUT` and exact absolute
`NQ_NG_BIN`, `NIGHTSHIFT_BIN`, `NIGHTSHIFT_RESOLVER_BIN`. It exercises real native
evaluation and retention over explicitly synthetic realization testimony.

GLASS-HERON packet preparation requires a new `RETIREMENT_PACKET_OUT`, absolute
`RETIREMENT_SESSION_MANIFEST`, its independently supplied
`RETIREMENT_SESSION_MANIFEST_SHA256` (`sha256:` form),
`RETIREMENT_PORTER_PROFILE`, and `RETIREMENT_AG_PRODUCER_BIN`. Generated fixture
and local custody live beneath the new output; no frozen historical fixture or
session is modified. The existing three-stage capacity remains fixed. Source
executor/Porter code is read and its exact identity retained, not executed.

Preparation records do not verify a human start, qualify a session, or grant
execution. The runner's `GH_START` must be supplied from the actual separately
verified operator decision; preparation no longer fabricates a start assertion
from the historical BRASS-RABBIT campaign. A session-shaped local test fixture
tests packet mechanics only, never real provider execution or session custody.

Run `python3 -B qualification/governed-campaign-loop-v1/test_modern_generator_boundary.py`.
Replacement qualification additionally requires actual modern NQ/Nightshift→AG
cross-office witnesses with classic unavailable and exact-revision review.
